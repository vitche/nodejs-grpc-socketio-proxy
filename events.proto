syntax = "proto3";

package events;

service Stream {
    rpc on(stream Message) returns (stream Message){}
    rpc emit(Message) returns (Message){}
}

message Message {
    string event = 1;
    string value = 2;
}